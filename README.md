mvn dependency:resolve after pulling to install gitignored target (dependencies)
mvn test -Dtest={Class Name}
mvn test -Dtest={Class Name}#{testMethod}
mvn test -Dgroups={groupName} for @Test(groups="{groupName}")):
